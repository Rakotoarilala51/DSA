#include <bits/stdc++.h>

using namespace std;

void solve(vector<int>& a, vector<int>& b){
    for(int i=0; i<a.size(); i++){
        
    }
}

int main()
{
    // WRITE YOUR CODE HERE, GETTING THE INPUT WITH STDIN AND OUTPUT ON STDOUT
    int t;
    string s;
    getline(cin,s);
    t=stoi(s);
    
    while(t--){
        int n;
        cin>>n;
        vector<int> a(n),b(n);
        for(int i=0; i<n;i++){
            cin>>a[i];
        }
        for(int i=0; i<n;i++){
            cin>>b[i];
        }
    }
    return 0;
}
